// Welcome to your new AL extension.
// Remember that object names and IDs should be unique across all extensions.
// AL snippets start with t*, like tpageext - give them a try and happy coding!

/*
Solicitud de transferencia
    -> obligar a lanzar
Lista de órdenes de transferencias retail (salida)
    -> Impedir lanzar o volver a abrir
    -> Filtrar al cambiar el estado Retai
            -Estado venta al por menor
    ->Impedir cambios desde pagina
Lista de órdenes de transferencias retail (entrada)
    -> impedir todo menos recibir
*/
/*
Interface EBS
No usar ajustes inventario (SIn journals)

*/